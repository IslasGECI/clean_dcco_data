name: Tests
on: push
jobs:
  actions:
    runs-on: ubuntu-latest
    steps:
    - name: Copia repositorio
      uses: actions/checkout@main
    - name: Construye imagen
      run: docker build --tag islasgeci/clean_dcco_data .
    - name: Verifica el formato
      run: docker run islasgeci/clean_dcco_data make check
    - name: Corre pruebas y evalúa cobertura
      run: docker run islasgeci/clean_dcco_data make coverage
    - name: Corre mutation testing
      run: docker run islasgeci/clean_dcco_data make mutants
    - name: Create/Update tag
      uses: rickstaa/action-create-tag@main
      with:
        tag: "latest"
        message: "Latest release"
        force_push_tag: true
